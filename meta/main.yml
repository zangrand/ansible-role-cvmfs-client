unpacked_info:
  author: Marco Verlato
  description: Ansible role to setup a CernVM-FS client
  company: INFN
  license: Apache
  min_ansible_version: 2.0
  platforms:
  - name: Ubuntu
    versions:
    - jammy
  #- name: EL
  #  versions:
  #  - 7
  unpacked_tags:
    - cvmfs
    - filesystem
dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
